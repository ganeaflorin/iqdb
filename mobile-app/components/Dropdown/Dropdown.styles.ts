import { themeColor } from '@/constants/Colors';
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  dropdown: {
    color: '#fff',
    backgroundColor: themeColor,
    borderColor: '#fff',
  },
  text: { color: '#fff', backgroundColor: themeColor },
  container: {
    borderColor: '#fff',
    backgroundColor: themeColor,
  },
});
