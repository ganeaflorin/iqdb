import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginTop: 60,
    gap: 30,
    minHeight: 380,
  },
  noQuestionsText: {
    alignSelf: 'center',
    textAlign: 'center',
  },
});
